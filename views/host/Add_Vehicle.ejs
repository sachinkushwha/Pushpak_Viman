<%- include('../partial/head') %> 

    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background: linear-gradient(135deg, #1e3c72, #2a5298);
        justify-content: center;
        align-items: center;
        height: 100vh;
      }
      .container {
        width: 100%;
        max-width: 800px;
        margin: 75px;
        justify-self: center;
        background:   rgba(255, 255, 255, 0.8); 
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 15px;
      }
      label {
        font-weight: bold;
        margin-bottom: 5px;
        color: #555;
      }
      input,
      select,
      textarea {
        padding: 10px;
        width: 100%;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 16px;
      }
      button {
        padding: 12px;
        background: linear-gradient(135deg, #007bff, #0056b3);
        color: white;
        border: none;
        cursor: pointer;
        font-size: 18px;
        border-radius: 5px;
        transition: background 0.3s ease;
      }
      button:hover {
        background: linear-gradient(135deg, #0056b3, #003d82);
      }
      h2,
      h3 {
        text-align: center;
        color: #333;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2><%=editing?"Update your Registration details":" Vehicle Registration Form 🚙"%></h2>
      <form action="<%=editing?'/Update-Vehicle':'/Add-Vehicle'%>" method="post">
        <h3>🚘 Vehicle Information</h3>
        <label for="vehicle_type">Vehicle Type:</label>
        <select id="vehicle_type" name="vehicle_type" required>
          <option value="Car" <%= editing && findcar.Vahical === 'Car' ? 'selected' : '' %>>Car</option>
          <option value="Truck" <%= editing && findcar.Vahical === 'Truck' ? 'selected' : '' %>>Truck</option>
          <option value="Bike" <%= editing && findcar.Vahical === 'Bike' ? 'selected' : '' %>>Bike</option>
        </select>
<input type="hidden" name="id" id="" value="<%=editing?findcar.id:''%>">
        <label for="vehicle_color">Color:</label>
        <input type="text" id="vehicle_color" name="vehicle_color" value="<%=editing?findcar.Color:''%>" />

        <label for="vehicle_number">Vehicle Number:</label>
        <input type="text" id="vehicle_number" name="vehicle_number" value="<%=editing?findcar.Vehicleno:''%>" required />

        <label for="rate_per_day">Rate/Day (₹):</label>
        <input type="number" id="rate_per_day" name="rate_per_day" value="<%=editing?findcar.Rate:''%>" required />

        <h3>👤 Owner Information</h3>
        <label for="owner_name">Owner Name:</label>
        <input type="text" id="owner_name" name="owner_name" value="<%=editing?findcar.Owner:''%>" required />

        <label for="owner_contact">Contact Number:</label>
        <input type="tel" id="owner_contact" name="owner_contact" value="<%=editing?findcar.Contact:''%>" required />

        <label for="owner_address">Address:</label>
        <textarea id="owner_address" name="owner_address" rows="3" placeholder="Enter Your Full Address" ><%=editing?findcar.Address:''%></textarea>

        <button type="submit"><%=editing?"Save Update":"🚀 Submit"%></button>
      </form>
    </div>
  </body>
</html>
